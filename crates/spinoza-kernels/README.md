# spinoza-kernels

Reserved crate for future low-level compute kernels.

This crate is a placeholder in the current stage. It will house optimized element-level kernels (e.g. SIMD-vectorized quadrature evaluation, batched tensor contractions) as the framework evolves beyond the Q1 structured-mesh scope.
